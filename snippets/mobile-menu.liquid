<div id="hamburger-button" class="hamburger-button">
<svg class="menu_icon_open" width="20px" height="20px" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
<path fill-rule="evenodd" clip-rule="evenodd" d="M4 5C3.44772 5 3 5.44772 3 6C3 6.55228 3.44772 7 4 7H20C20.5523 7 21 6.55228 21 6C21 5.44772 20.5523 5 20 
5H4ZM3 12C3 11.4477 3.44772 11 4 11H20C20.5523 11 21 11.4477 21 12C21 12.5523 20.5523 13 20 13H4C3.44772 13 3 12.5523 3 12ZM3 18C3 17.4477 3.44772 17 4 17H20C20.5523 
17 21 17.4477 21 18C21 18.5523 20.5523 19 20 19H4C3.44772 19 3 18.5523 3 18Z" fill="currentcolor"/>
</svg>
<svg class="menu_icon_close" width="20px" height="20px" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M19.207 6.207a1 1 0 0 0-1.414-1.414L12 10.586 6.207 4.793a1 1 0 0 0-1.414 1.414L10.586 12l-5.793 5.793a1 1 0 1 0 1.414 1.414L12 13.414l5.793 5.793a1 1 0 0 0 1.414-1.414L13.414 12l5.793-5.793z" fill="currentcolor"/></svg>
      </div>
<nav id="wdt-mobile-menu" role="navigation" class="wdt-mobile-menu"> 
  <ul>
    {%- for link in linklists[mobileMenu].links -%}
      <li>        
        <a href="{{ link.url }}" class="parent-link" {% if link.active %}aria-current="page"{% endif %}>
          {{ link.title }}
          {%- if link.links != blank -%}
          <div class="icon_arrow_sec"></div>
        {%- endif -%}
        </a>        
        {%- if link.links != blank -%}
          <ul class="child-menu">
            {%- for child_link in link.links -%}
              <li>
                <a href="{{ child_link.url }}" class="child-link" {% if child_link.active %}aria-current="page"{% endif %}>
                  {{ child_link.title }}
                    {%- if child_link.links != blank -%}
                       <div class="icon_arrow_sec"></div>
                    {%- endif -%}
                </a>
                {%- if child_link.links != blank -%}
                  <ul class="grandchild-menu">
                    {%- for grandchild_link in child_link.links -%}
                      <li>
                        <a href="{{ grandchild_link.url }}" class="grandchild-link" {% if grandchild_link.active %}aria-current="page"{% endif %}>
                          {{ grandchild_link.title }}
                        </a>
                      </li>
                    {%- endfor -%}
                  </ul>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </ul>
        {%- endif -%}
      </li>
    {%- endfor -%}
  </ul>
</nav>
<style>
.hamburger-button {
  /* display: none; */
  font-size: 2em;
  background: none;
  border: none;
  cursor: pointer;
  position: absolute;
  top: 32px;
  right: 15px;
  z-index: 1000;
  background: transparent;
}
.wdt-mobile-menu {
  position: fixed;
  top: 0;
  right: 0;
  width: 100%;
  height: 100%;
  background: white;
  overflow-y: auto;
  z-index: 999;
  transform: translateX(-100%);
    transition: all 0.4s ease;
  padding: 2rem;
}
  .hamburger-button svg {
    width: 1.6rem;color: var(--DTBodyBGColor);
    height: 2rem;
}
  button.hamburger-button:hover{
    background: transparent; border: 0px solid transparent;
  }
  .wdt-mobile-menu ul li {
    padding: .5rem 0;
}
  .wdt-mobile-menu ul li > a {
    display: flex;
    align-items: center;
    justify-content: space-between;
}
  .hamburger-button.active {
    position: fixed;
}
  button.hamburger-button svg line{
    transition: all 0.3s linear;
  }
  svg.menu_icon_close {
    position: absolute;
    right: -10px;
    /*top: 55%;
    transform: translateY(-55%); */
    transition: all 0.3s linear;
    visibility: hidden;
    opacity: 0;
}
  svg.menu_icon_open{
    transition: all 0.3s linear;
  }
  .hamburger-button.active svg.menu_icon_open{
    visibility: hidden;
    opacity: 0;
  }
  .hamburger-button.active svg.menu_icon_close{
    visibility: visible;
    opacity: 1;
  }
  ul.child-menu > li {
    display: none;
  }
  a.parent-link.active + ul.child-menu > li{
  animation: fadeInUp 1s ease-out .05s both;
    display: block;
  }
  .icon_arrow_sec {
    position: relative;
}
  .icon_arrow_sec:after {
    content: "";
    background: white;
    width: 15px;
    height: 2px;
    right: 0;
    margin: auto;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    transition: all .3s linear;
    opacity: 1;
}
  .icon_arrow_sec:before {
    content: "";
        height: 15px;
        width: 2px;
        position: absolute;
        right: 6px;
        background: white;
        margin: auto;
        top: 50%;
        transform: translateY(-50%);
        transition: all .3s linear;
        opacity: 1;
}
  a.parent-link.active .icon_arrow_sec:before,
  a.child-link.active .icon_arrow_sec:before{
    transform: translateY(-50%) rotate(90deg);
}
  @keyframes fadeInUp{
   0% {
    opacity: 0;
    transform: translate3d(0, 15%, 0);
}
  100%{
    opacity: 1;
    transform: none;
  } 
  }

  @media (min-width: 1201px){
    .hamburger-button{
      display: none;
    }
  }
@media (max-width: 767px) {
  .hamburger-button {
    display: block;
  }
  .wdt-mobile-menu.active {
    transform: translateX(0);
  }
  .wdt-mobile-menu.active {
    background:var(--DT_Button_BG_Hover_Color);
  }
  .wdt-mobile-menu ul.child-menu,
  .wdt-mobile-menu ul.grandchild-menu {
    display: none;
  }
  .wdt-mobile-menu .parent-link.active + .child-menu,
  .wdt-mobile-menu .child-link.active + .grandchild-menu {
    display: block;
  }
}
  .wdt-mobile-menu.active {
    transform: translateX(0);
  }

  .wdt-mobile-menu a:hover{
        color: var(--DTLinkHoverColor);

  }
</style>
    <script>
   document.addEventListener('DOMContentLoaded', function () {
  let hBtn = document.querySelectorAll('#hamburger-button, #sticky-hamburger-button'); // Target both regular and sticky hamburger buttons
  let menus = document.querySelectorAll('#wdt-mobile-menu, #sticky-wdt-mobile-menu'); // Target both menus

  hBtn.forEach(button => {
    button.addEventListener('click', function (event) {
      menus.forEach(menu => menu.classList.toggle('active'));
      hBtn.forEach(btn => btn.classList.toggle('active'));
    });
  });

  document.querySelectorAll('.wdt-mobile-menu .parent-link, .sticky-wdt-mobile-menu .parent-link').forEach(function (parentLink) {
    parentLink.addEventListener('click', function (event) {
      event.preventDefault();
      toggleMenu(parentLink, '.child-menu');
    });
  });

  document.querySelectorAll('.wdt-mobile-menu .child-link, .sticky-wdt-mobile-menu .child-link').forEach(function (childLink) {
    childLink.addEventListener('click', function (event) {
      event.preventDefault();
      toggleMenu(childLink, '.grandchild-menu');
    });
  });

  function toggleMenu(link, menuClass) {
    link.classList.toggle('active');
    let menu = link.nextElementSibling;
    if (menu && menu.matches(menuClass)) {
      menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
    }
  }
});

        </script>